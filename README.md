# nested-download

```bash
supabase --profile supabase-staging functions deploy demo
supabase --profile supabase-staging functions download demo
```

Observe that a nested directory is created: `supabase/functions/demo/functions/shared`
